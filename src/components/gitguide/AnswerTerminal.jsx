import React from 'react';

function AnswerTerminal(props) {
    // if (props.score === true) {
        return (
            <p>{props.terminal}</p>
        );
    // }
}

export default AnswerTerminal;