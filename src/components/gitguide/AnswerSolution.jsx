import React from 'react';

function AnswerSolution(props) {
    // if (props.score === true) {
        return (
            <p>{props.solution}</p>
        );
    // }
}

export default AnswerSolution;